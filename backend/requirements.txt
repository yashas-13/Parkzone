# ParkZone API - runtime dependencies (pinned; install with: pip install -r requirements.txt)
# Verified working on Ubuntu 22.04 (VPS) and Termux/aarch64 (local dev).
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
python-dotenv==1.2.3
pymongo==4.18.1
